export enum AvatarImageDataAttributes {
  /**
   * Present when the image is loading.
   */
  loading = 'data-loading',
  /**
   * Present when the image has loaded.
   */
  loaded = 'data-loaded',
  /**
   * Present when the image has failed to load.
   */
  error = 'data-error',
}
